apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-chat-session
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: microk8s-hostpath
  resources:
    requests:
      storage: 1Gi
